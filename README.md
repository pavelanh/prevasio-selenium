# Test Automation Framework for Prevasio CSPM Scanner

1. Install all the dependencies.
    ```bash
    pip install -r requirements.txt
    ```

2. Run the `login_test`
    ```bash
    python -m unittest tests/login_test.py
    ```
   